# Hypothesis backlog — survey-monotonicity-ml

Ideas not yet promoted to a hypothesis folder. Promote a row via
`hypothesis-exploration`; a promoted row becomes
`hypotheses/<YYYY-MM-DD-slug>/`. For this survey, the **empirical-benchmark
claims** are the hypotheses — each becomes a run under the `benchmarks/` harness.

| idea | origin | note |
|---|---|---|
| Under a unified protocol, hard-constrained architectures match/beat soft & regularization methods at equal guarantee | pitch pillar 2 (empirical study) | needs rival-method re-impl in harness |
| Partial-monotonicity support + training cost separate the families empirically, not just in principle | pitch pillar 2 | design ablation across families |
| _(add rows as the taxonomy + benchmark scope firm up)_ | | |
