# Migrate documentation from MkDocs to Sphinx

> **Note for the author:** This file is the source for the PR description. Copy
> the body below into the GitHub PR. After the PR is open, this file can be
> deleted (it's not referenced by anything in the docs build).

---

## Summary

Replaces the MkDocs + `mkdocs-material` documentation stack with **Sphinx +
PyData Sphinx Theme + `sphinx-autodoc2` + `myst-nb` + `sphinx-multiversion`**.

### Why now

- **MkDocs is unmaintained** (no releases since August 2024) and the author has
  announced an incompatible "MkDocs 2.0" that will break every plugin and theme.
  The build currently emits a noisy warning urging migration.
- **`mkdocs-material`'s maintainer is also exiting** to new projects
  (MaterialX / Zensical), so staying on Material is a delayed exit, not an
  alternative.
- **Sphinx is the format of the Python ML ecosystem `mononet` targets** —
  PyTorch, JAX, Keras, NumPy, SciPy, scikit-learn all use it. Aligning with
  that ecosystem also unlocks `intersphinx` cross-links into those projects'
  docs from our API pages.

Specs and plan:
- [docs/superpowers/specs/2026-05-22-sphinx-migration-design.md](docs/superpowers/specs/2026-05-22-sphinx-migration-design.md)
- [docs/superpowers/plans/2026-05-22-sphinx-migration.md](docs/superpowers/plans/2026-05-22-sphinx-migration.md)

## What changes

### Stack swap

| Concern | Before | After |
| --- | --- | --- |
| Engine | `mkdocs` | `sphinx>=8.1` |
| Theme | `mkdocs-material==9.7.6` | `pydata-sphinx-theme>=0.16` |
| API docs | `mkdocstrings[python]` + custom 320-line `docs/create_api_docs.py` | `sphinx-autodoc2>=0.5` (auto-mode) |
| Notebooks | `mkdocs-jupyter` | `myst-nb>=1.1` |
| Versioning | `mike` | `sphinx-multiversion>=0.2` |
| Navigation | hand-edited `SUMMARY.md` via `mkdocs-literate-nav` | `toctree` directives in `docs/index.md` |
| Authoring | Markdown + pymdownx extensions | MyST Markdown (`colon_fence`, `dollarmath`, `amsmath`, `linkify`, `tasklist`, `deflist`, `fieldlist`, `substitution`) |
| Build CLI | 125-line `docs/docs.py` Typer wrapper | stock `sphinx-build` / `sphinx-autobuild` |
| Pre-commit | `tools/build-docs.sh` → `mkdocs build` | `tools/build-docs.sh` → `sphinx-build -W` |
| CI deploy | `mike deploy --push` | `sphinx-multiversion` + `peaceiris/actions-gh-pages@v4` |

### Directory layout

`docs/docs/*` content is moved one level up to `docs/*`. After the merge:

```
docs/
├── conf.py                # Sphinx config (replaces docs.py + create_api_docs.py + mkdocs.yml)
├── index.md               # Landing page with toctree
├── guides/                # PyTorch, JAX, Keras
├── concepts/              # Monotonicity, layers
├── benchmarks/            # 1 notebook
├── about/                 # License, changelog, citation
├── contributing.md
├── apidocs/               # GENERATED by sphinx-autodoc2 (gitignored)
├── _static/               # extra.css (PyData-themed brand colours), extra.js, logo.png
├── _templates/            # layout.html — OG/Twitter meta tags
├── superpowers/           # internal specs/plans (excluded from build)
└── _build/                # output (gitignored)
```

### Deleted

`docs/mkdocs.yml`, `docs/docs.py`, `docs/create_api_docs.py`, `docs/__init__.py`,
`docs/overrides/{404,main}.html`, `docs/docs/navigation_template.txt`,
`docs/docs/` directory itself.

### Added

- `tools/gen_versions_json.py` — generates `versions.json` for the PyData theme
  version switcher (since `sphinx-multiversion` doesn't emit one natively).

### Capability changes

**Gained:**
- Inline math (`$\mathbb{R}^n$`) and AMS environments via MyST `dollarmath` /
  `amsmath`.
- Intersphinx cross-links into PyTorch, JAX, NumPy docs from API pages — e.g.,
  a `torch.Tensor` annotation in a docstring is now a clickable link to PyTorch's
  official docs.
- Multi-version docs use a JSON-driven switcher dropdown (PyData theme).

**Lost (tracked as follow-ups):**
- Google-style docstring `Args:` / `Returns:` sections currently render as flat
  prose. `sphinx-autodoc2` has no Google parser — it accepts only `rst`, `myst`,
  or a class FQN. There's an inline TODO at [docs/conf.py](docs/conf.py) marking
  this as future work (either a custom parser, a griffe-based plugin, or a
  source-side rewrite). 3 docstrings in 2 files are affected today.
- `mkdocs-glightbox` image zoom and `mkdocs-minify-plugin` — both small UX
  features that PyData theme doesn't ship; not on the spec's must-have list.
- `keras` intersphinx target — `keras.io`'s inventory URL 404s; entry commented
  out with a TODO. (PyTorch, JAX, NumPy, Python all work.)

## Migration strategy

The plan was designed so **every commit leaves the pre-commit `docs` hook
green**. 16 commits in 6 phases:

| Phase | Commits | Outcome |
| --- | --- | --- |
| 1. Bring up Sphinx alongside MkDocs | 145b9ad → f5afc1e | Sphinx `-W`-clean against `docs/docs/` source; MkDocs still authoritative for pre-commit |
| 2. Swap pre-commit to Sphinx | 8bb772f | Pre-commit now invokes Sphinx; MkDocs files still present but unused |
| 3. Remove MkDocs; move content up | 7dc8b32 → 7b2fec6 | MkDocs gone; content at `docs/*`; `docs/superpowers/` excluded from build |
| 4. Polish | 5da8d8c → d22f13a | Cross-refs converted to MyST `{py:class}`; `-W` restored; OG meta tags ported; PyTorch URL fixed |
| 5. Versioning + CI | 2a5362a → 2ad05fc | `versions.json` generator added; GitHub Actions workflow switched to `sphinx-multiversion` + `peaceiris/actions-gh-pages@v4` |
| 6. Documentation + final fixes | e1aa397 → 50b009e | `CLAUDE.md` updated; changelog `{include}` directive fixed; brand colours in `extra.css`; `DOCS_VERSION` env in CI; boolean `preferred` in `versions.json` |

## Test plan

After this PR merges to `main`, the first deploy job will exercise the full
pipeline. Concrete checks for the reviewer:

### Local

- [ ] `uv sync --group docs` succeeds against the new dependency group.
- [ ] `./tools/build-docs.sh` exits 0 — Sphinx `-W` clean against the live tree.
- [ ] `./tools/serve-docs.sh` starts `sphinx-autobuild` on
      `http://0.0.0.0:8008`; saving a `.md` file or a `mononet/*.py` source file
      triggers a rebuild.
- [ ] `uv run pytest -q`, `uv run mypy`, `uv run ruff check`,
      `uv run pre-commit run --all-files` all pass.

### Build output (open `docs/_build/html/index.html` in a browser)

- [ ] Landing page renders with PyData theme; light/dark toggle works; the
      mononet brand colours appear (navbar, links).
- [ ] Every section in the toctree is reachable: Getting started (3 framework
      guides), Concepts (2 pages), Benchmarks (1 notebook + index), API
      reference (auto-generated), About (3 pages), Contributing.
- [ ] `docs/about/changelog.html` shows the actual changelog content (not the
      literal `{include}` directive — this was a bug caught in final review).
- [ ] On a guide page (e.g., `guides/pytorch.html`), `MonoLinear` is a clickable
      cross-reference into the auto-generated API page.
- [ ] On any API page, a `torch.Tensor` annotation is a clickable
      cross-reference to the PyTorch docs (intersphinx).
- [ ] Code blocks have a copy button (`sphinx-copybutton`).
- [ ] OG / Twitter Card meta tags are present in every page (used by social
      link previews — see `docs/_templates/layout.html`).

### CI

- [ ] The `Docs` GitHub Actions workflow runs to green on this PR.
- [ ] After merge, the first `main` push deploys to GitHub Pages at
      `https://davorrunje.github.io/mononet/`. The site root redirects to
      `/main/`. The version-switcher dropdown shows a single "dev (main)" entry
      (versioned releases will appear once tags exist).
- [ ] The `dependabot` config now tracks Sphinx-family packages; no further
      MkDocs-family PRs should appear.

## Reviewer notes

- **Most commits are small and one-purpose.** Reading them in order (oldest
  first) shows the migration phasing — each commit is a coherent unit.
- **`docs/conf.py` is the central artifact** — single 113-line file replaces
  three deleted files totalling ~600 lines.
- **The `superpowers/` directory** under `docs/` contains the spec + plan +
  this PR description. It is excluded from the Sphinx build via
  `exclude_patterns = ["_build", "superpowers"]` in `conf.py`. Internal-only.
- **Outstanding TODOs** are documented inline in `docs/conf.py` (Google-style
  docstring parser; `keras` intersphinx URL once available).

## Follow-up issues (not blocking)

- Restore Google-style docstring rendering (custom autodoc2 parser, griffe
  plugin, or `sphinx.ext.napoleon` adapter).
- Add `keras` intersphinx target when `keras.io` publishes a stable
  `objects.inv` URL.
- Once the first `v*.*.*` tag exists, verify the multiversion build correctly
  marks the tagged release as `preferred` in `versions.json` and that the
  PyData theme's "outdated version" banner behaves correctly.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
